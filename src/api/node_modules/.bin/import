#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_import@0.0.6@import/import" "$@"
  ret=$?
else 
  node  "$basedir/../_import@0.0.6@import/import" "$@"
  ret=$?
fi
exit $ret
